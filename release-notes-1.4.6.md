## v1.4.6

### 长任务可观测性（goal 运行不再「莫名卡死 / 莫名中断」）
- **goal 空闲看门狗不再误杀执行中的工具**：长命令（视频批生成等）运行期间 SSE 静默超过空闲阈值时，看门狗先检查是否有工具在执行，有则续期而不是关流（此前 600s 静默 terminal 与 600s 看门狗同秒触发，后续十几分钟的 agent 产出全部丢失）
- **孤儿工具卡自动收敛**：run 结束时仍在「执行中」的工具卡标记为「已中断」，不再永久转圈
- **goal 轮次边界不再广播 idle**：每轮 judge + 静默工具期间底部状态行/spinner 不再凭空消失
- **已完成轮次的深度推理块正确显示「已完成」**：不再永远显示「思考中…」
- **状态文字不再被残留思考文本吞掉**：长工具执行期间「正在调用工具/生成回复」恒常显示
- **terminal 前台超时 600s → 1h**：视频批量生成不再被拦腰砍断

### 同会话消息排队（告别「未收到模型输出」空占位）
- **gateway busy_input_mode 切 queue**：忙时新消息走 FIFO 按到达顺序逐条执行，绝不再打断正在跑的轮次（默认 interrupt 模式下，连发消息会互相顶掉、留下一串空占位气泡）

### 微信授权原路返回
- **微信驱动的任务需要授权时，审批请求直接发回微信**：不再弹桌面端（人不在电脑前 = 永久卡死）。微信里回复「批准 / 拒绝」（支持 Y/N、approve/deny 等），agent 原地继续或停止；4 分钟无回复按超时处理并通知。桌面会话的审批弹窗行为不变

### 工作空间
- 工作空间菜单的最近列表支持逐条删除（hover 出 ×，不动磁盘）
- **会话创建前即可绑定工作空间**：首次发送时草稿绑定自动落到新会话，首轮即生效

### 其他
- 评审/自检事件改为消息流内联卡片，移除 Composer 上方的「评审摘要」折叠框
- 会话标题/预览不再泄漏「未收到模型输出」占位符

---

**SHA**: 见 GitHub Release 资产。解压后运行 `Abcyesno.exe`（win-unpacked 便携版，无安装器）。

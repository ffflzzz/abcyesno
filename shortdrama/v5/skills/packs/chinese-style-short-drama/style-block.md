# chinese-style-short-drama 风格块（真人国风短剧）

# 这段文本作为**视觉风格段**注入每一镜提示词（`style.load` → pipeline 的 `_style_block`
# → `prompt.style_block_line`）。核心：真人国风短剧——精致瓷肌、华丽妆造、电影级柔光，
# 但仍是**真人实拍感**，不是动漫、也不是廉价 CG。
#
# 长度纪律（照抄 shortdrama 的实测结论）：**超过 200 字会压掉画面内容**
#   —— 主体、动作、场景全丢，模型只顾着满足风格描述。
#   源 skill 那一整节（STEP 3「嵌入参考短剧画面风格逻辑」）约 900 字，
#   **不能原样搬**；2026-09-16 移植时已按此压缩，注入正文 ≈ 187 字。
#   复核方法：`python -c "from v5, media import style; print(len(style.wrap(style.load(root))))"`。
#
# ★ 源文里三条**有意没搬进来/已改写**的内容，理由如下（改动前先读）：
#
#   1. **具体光源**（烛光 / 灯笼 / 花窗投射光 / 丁达尔光束 / 蜡烛光晕）——
#      **光源全片只有一个出处**：归「场景」列的资产描述（`assets.scene_lines()` 注入）。
#      在这里硬编码会与场景锚点直接打架。实测（lost-and-found LN24）：风格块写
#      「夜戏以暖黄白炽灯与火光为主」× 场景卡写「冷白荧光偏青」→ 成片整段漂成
#      暖光室内（书架/吊灯/木桌），**4 张 location 参考图全部白生成**。
#      故本段只声明"光源以本镜场景描述为准"，不指定任何具体灯的形态与色温。
#
#   2. **具体头饰名词**（发冠 / 步摇 / 流苏）——2026-09-22 实测事故（bumengzhe）：
#      旧句「复杂发髻与发冠、步摇流苏」逐镜注入，等于给**每个角色**都下了戴冠指令。
#      落魄赌徒老周（角色卡：发髻只用一根黑木旧簪）被画成绿松石金冠+步摇，
#      重画一次仍然漂（概率性偏向全局句）；**空镜**（茶摊，无人物）甚至长出一个
#      坐在摊边戴冠的人物——概念词在提示词里出现就会被渲染（与烧字/分屏同一条
#      规律，本注释块第 3 条）。角色妆造的权威源是**角色卡与画面描述**
#      （identity_line 锚点注入），全局句只做委托声明，不点名任何具体饰物。
#
#   3. **「竖屏优先用面部特写 / 眼部特写 / 饰品极特写」**——与本管线**实测能力边界冲突**：
#      分镜写「特写」实测一律被画成**人物中景**（六变体全失败：去掉场景锚点 / 去掉
#      `@角色` 引用 / 把景别强调挪到句末 / 不传参考图 —— 都是中景；人眼复核确认，
#      不是 QC 误判）。这是生成模型的能力边界，**改措辞无解**。
#      构图纪律因此**下沉到本包 `scenedesigner/SKILL.md`**（并要求用「近景 + 手与道具
#      同框」替代极特写），不放进逐镜风格块。
#
# ⚠️ 本段会同时注入**静帧**与**视频**提示词（`prompt.py` 三处都在用）。所以只写
#   「画面看起来该是什么样」，不要写"眨眼/运动"这类**视频层表演**指令——那属于
#   scenedesigner 的 per-second 面板（见本包 scenedesigner/SKILL.md）。

视觉风格：真人国风短剧，古风实拍电影感——面部为精致BJD瓷肌质感，皮肤干净并带柔和面部高光；发型、头饰与服装严格遵循各角色的身份描述，做工精细，丝绸与锦缎刺绣质感；发丝与衣袖有轻微随风微动；电影化柔光配侧逆光轮廓光，浅景深、焦外光斑。光源与色温以本镜「场景」描述为准。

绝对不要：纯动漫插画、扁平动漫脸、廉价网游CG、低精度服装、塑料皮肤、均匀平铺光。

表面渲染：所有表面都是真实材质的自然呈现。

## v1.4.9

### 修复微信端高频报错「gateway request timeout: prompt.submit」
- **根因**：Hermes 网关的 WebSocket 读循环被一条慢请求**串行阻塞**，后续所有请求（连只做内存操作的轻量请求也一样）全被堵在缓冲区里排队超时。微信侧表现成反复报「Hermes 正忙于长任务，初始化排队超时」——即使当时并没有长任务在跑
- **修复**：请求分发与读循环解耦，慢请求不再阻塞后续请求的处理
- **超时自动重试**：网关临时拥塞导致的提交超时，自动在同一会话重试一次，不再直接把错误抛给微信用户

### Agent 待办清单（todos）
- **对话内嵌待办气泡**：agent 规划多步任务时，聊天流中实时展示待办清单，逐步划线推进（进行中 / 已完成 / 已取消）
- **微信侧进度文案**：微信端同步推送「📋 计划 N 步」「✅ x/N 已完成」等进度提示（已去重限流，不会刷屏）
- **模型现在会主动规划**：通用对话场景也注入待办规划引导。此前该引导只在代码工作区生效，导致聊天/创作场景下模型从不自发使用待办清单

---

**SHA**: 见 GitHub Release 资产。解压后运行 `Abcyesno.exe`（win-unpacked 便携版，无安装器）。

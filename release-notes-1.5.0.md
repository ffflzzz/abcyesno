## v1.5.0

### 新增：短剧生产模块
- **短剧工作台**：启动台新增「短剧」入口，一站式完成从分集大纲到成片的全流程创作
- **前端界面**：React SPA 集成在内置浏览器标签页中，与主应用同源加载，无需额外配置
- **创作链后端**：内置 v5 创作链引擎，按需自动启动，不占用应用启动时间
- **依赖自检**：`npm run shortdrama:venv` 可在本机一键重建运行环境

### 修复：内置浏览器地址栏无法输入
- **根因**：地址栏每 1 秒被自动刷新覆盖。程序要同步网页地址，却分不清「用户正在打字」和「网页跳转了」——你敲第一个字，它判定页面已跳转，随即把内部空白页地址回填进输入框
- **修复**：输入内容与网页真实地址分开存储，打字不再被误判为跳转；同时让「正在打字」的判定实时生效（原判定读取的是过期状态，等于没写）
- **体验优化**：内部占位地址不再显示为长串乱码、点击输入框自动全选、按 Esc 取消输入

### 修复与优化（累积）
- **网络**：注入系统代理时自动绕过自家模型端点，解决 Clash 开启时微信端反复报「API failed after 3 retries」
- **质量门禁**：新增未定义引用静态扫描（跨作用域引用检查），防止部分分支才暴露的运行时崩溃
- **论文重写**：模型调用凭据改为从环境变量读取，不再硬编码

---

**SHA**: 见 GitHub Release 资产。解压后运行 `Abcyesno.exe`（win-unpacked 便携版，无安装器）。

## v1.5.1

短剧模块的一次集中增强：两个新风格包、旁白自动化、多密钥并发提速。

### 新增：启动台加入 OpenRouter
- 启动台多一个入口，直达 OpenRouter 官网（模型比价 / 充值），走内置浏览器标签页

### 短剧：两个新风格包
- **半解说真人短剧**：画面 + 画外音解说的形式。配套的旁白轨、角色卡结构、场景与道具锚定契约一并补齐
- **都市残酷惊悚**：命案题材包（"打小人"项目的类型基础），含复合镜、场景锁定、叙事可读性等契约

### 短剧：旁白可以自动生成了
- 画外音交给视频模型在出片时一并合成，不用再单独配音、单独混合音轨
- 单独挂 TTS 混音默认关闭：实测两者同时开会出现两重声音。TTS 降级为合成失败时的回落手段

### 短剧：出图 / 出片更快
- **多密钥支持**：可配置多个模型密钥，按剩余额度自动挑"最早恢复"的那个，各自独立限速
- **队列满自动换密钥重试**：不再单个密钥死等，绕开单池拥堵
- **图片通道分离**：静帧与资产图改走高配额图片通道，不再和视频任务抢额度
- **文本通道切换**：走国内入口，实测稳定

### 修复
- **分镜分组回退**：此前把一次"切"当成"状态切换"，20 个镜头被错切成 18 组；改回按真实叙事切换点分组
- **组间连贯性**：新增状态锚点 + 跨组静帧链，组与组之间的人物与环境不再断档
- **风格包契约修正**：场景卡必须绑参考图并出环境全景空镜、复合镜写法规定、微表情只写近景及以上景别、道具形制以项目 brief 为唯一准（审片端加了交叉校验）
- **空镜豁免**：分镜里写明"空镜"的镜头不再强制绑主角、不再报人数异常

### 优化
- **审片报告瘦身**：只写问题清单、通过项一行带过，输出量约为原来的三分之一 —— 审片耗时主要花在写报告，不在看产物
- **死代码清理**：移除 8 个已被替换的界面组件与 12 处零引用 Python 代码，附审计报告与"禁止误删清单"

---

**SHA**: 见 GitHub Release 资产。解压后运行 `Abcyesno.exe`（win-unpacked 便携版，无安装器）。
